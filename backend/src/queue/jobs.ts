export type emailSyncJob = {
  emailId: string;
  fileName: string;
  size: number;
  location: string;
};

export type pdfExtraction = {
  invoiceId: string;
  location: string;
  size: number;
};

export type reconciliation = {};
